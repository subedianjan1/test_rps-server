RPS server  is the Remote power supply simulation application.

Test procedure:

Run RPS server with the command:
$ set FLASK_APP=rps-server.py
$ flask run

Command.py is the interface file that sends command to the server.


execute test_rps-server.py with the command:
pytest -v test_rps_sever.py








